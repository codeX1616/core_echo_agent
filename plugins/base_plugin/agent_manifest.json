{
    "name": "open_file",
    "description": "Opens a file on the host operating system.",
    "parameters": {
        "type": "object",
        "properties": {
            "file_path": {
                "type": "string",
                "description": "The absolute path to the file to open."
            }
        },
        "required": ["file_path"]
    },
    "destructive": false
}
